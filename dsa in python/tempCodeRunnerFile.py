 if (brackets=='(' and ch==')') or (brackets=='{' and ch=='}') or (brackets=='[' and ch==']'):
        #         continue
        #     else:
        #         return False
        # return len(stack)==0